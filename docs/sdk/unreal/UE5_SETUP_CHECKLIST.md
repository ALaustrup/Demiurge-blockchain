# UE5 Setup Checklist

## ✅ Pre-Flight Check

- [x] UE5 5.7.1 installed via Launcher
- [x] Visual Studio 2026 installed with C++ tools
- [x] Project file configured (`DemiurgeClient.uproject`)
- [x] C++ modules implemented
- [x] Blueprint widget classes ready

---

## 🚀 Step-by-Step Setup

### 1. Open Project in Unreal Editor

```
1. Launch Unreal Engine 5.7.1 (from Epic Games Launcher)
2. Click "Browse" or "Open"
3. Navigate to: x:\Demiurge-Blockchain\client\DemiurgeClient\
4. Select DemiurgeClient.uproject
5. Click "Open"
```

**First Time:** Editor will detect C++ code and ask to rebuild - click **Yes**

### 2. Generate Visual Studio Project Files

**If not auto-generated:**

```
Right-click DemiurgeClient.uproject → Generate Visual Studio project files
```

**OR in Editor:**
```
File → Refresh Visual Studio Project
```

**Expected Output:**
- Creates `DemiurgeClient.sln` in project directory
- Creates `Intermediate/` folder with build files

### 3. Compile C++ Modules

**Option A: From Editor (Recommended)**
```
Tools → Compile
```
- Watch Output Log for progress
- Wait for "Compile Complete" message

**Option B: From Visual Studio**
```
1. Open DemiurgeClient.sln in Visual Studio 2026
2. Set configuration: Development Editor
3. Build → Build Solution (Ctrl+Shift+B)
4. Return to Unreal Editor
```

**Expected:** All 3 modules compile:
- ✅ DemiurgeClient
- ✅ DemiurgeWeb3  
- ✅ QorUI

### 4. Verify Modules Loaded

**In Editor:**
```
Window → Developer Tools → Modules
```

**Check for:**
- `DemiurgeClient` - Status: Loaded
- `DemiurgeWeb3` - Status: Loaded
- `QorUI` - Status: Loaded

### 5. Create Blueprint Game Mode

```
1. Content Browser → Right-click → Blueprint Class
2. Search for "Demiurge Game Mode"
3. Name: BP_DemiurgeGameMode
4. Open Blueprint
5. Set Default Node URL = ws://127.0.0.1:9944
6. Set Auto Connect On Start = true
```

### 6. Create Blueprint Environment Manager

```
1. Content Browser → Right-click → Blueprint Class
2. Search for "Demiurge Environment Manager"
3. Name: BP_DemiurgeEnvironmentManager
4. Configure post-process settings
```

### 7. Create Blueprint Widgets

**Follow:** `docs/BLUEPRINT_UI_GUIDE.md`

**Quick Start:**
1. Create `WBP_QorIDLogin` (parent: Qor ID Login Widget)
2. Create `WBP_Wallet` (parent: Qor Wallet Widget)
3. Create `WBP_Inventory` (parent: Qor Glass Panel)

---

## 🧪 Testing

### Test 1: Module Compilation
- [ ] All modules compile without errors
- [ ] No missing includes or dependencies

### Test 2: Project Opens
- [ ] Editor opens successfully
- [ ] No missing module warnings
- [ ] Content Browser shows project assets

### Test 3: Blueprint Creation
- [ ] Can create Blueprint from C++ classes
- [ ] Widget Blueprints can be created
- [ ] No compilation errors in Blueprints

### Test 4: Network Connection (When Node Ready)
- [ ] WebSocket connects to node
- [ ] RPC calls succeed
- [ ] Username availability check works

---

## ⚠️ Common Issues

### Issue: "Missing Modules" Warning

**Solution:**
1. Close Editor
2. Delete `Binaries/` and `Intermediate/` folders
3. Regenerate Visual Studio files
4. Rebuild from Visual Studio
5. Reopen Editor

### Issue: C++ Compilation Errors

**Check:**
1. Visual Studio 2026 C++ tools installed
2. Windows SDK installed
3. All `#include` paths correct
4. Module dependencies in `*.Build.cs` files

### Issue: Blueprint Classes Not Found

**Solution:**
1. Ensure C++ code compiled successfully
2. Restart Editor
3. Check `*.generated.h` files exist
4. Verify `UCLASS(Blueprintable)` macro present

---

## 📁 Project Structure

```
DemiurgeClient/
├── Content/              # Blueprints, assets (create this)
│   ├── UI/
│   │   └── Blueprints/   # Widget Blueprints
│   ├── Blueprints/       # Actor Blueprints
│   └── Maps/            # Level files
├── Source/              # C++ source (already exists)
│   ├── DemiurgeClient/
│   ├── DemiurgeWeb3/
│   └── QorUI/
├── DemiurgeClient.uproject
└── DemiurgeClient.sln   # Generated by Editor
```

---

## 🎯 Next Actions After Setup

1. **Create Test Level**
   - Add `BP_DemiurgeEnvironmentManager` to level
   - Set as default Game Mode: `BP_DemiurgeGameMode`
   - Test post-process effects

2. **Create Login UI**
   - Add `WBP_QorIDLogin` to viewport
   - Test username input
   - Verify availability checking

3. **Test Network Connection**
   - Start Substrate node (external terminal)
   - Connect from UE5
   - Verify RPC calls work

---

*Ready to proceed with UE5 integration!*
