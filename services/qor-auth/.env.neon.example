# Optional hosted Postgres (Neon project id is private; keep it in this gitignored file).
# Copy to .env.neon (gitignored) and fill from the Neon console.
# Local Studio still uses Docker Postgres on 127.0.0.1:5432.

# QOR_AUTH__DATABASE__URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
