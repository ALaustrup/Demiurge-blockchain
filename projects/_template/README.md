# Studio project template

Copy this folder to `projects/YourGameName/` and edit `project.json`.

Start the stack from the repo root:

```powershell
npm run studio:health
```

See [docs/DEMIURGE_STUDIO.md](../../docs/DEMIURGE_STUDIO.md).
