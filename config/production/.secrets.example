# Demiurge Production Secrets Template
# Copy to .secrets and fill with real values
# Generate with: ./scripts/deploy.sh generate-secrets

# JWT Secrets - Generate with: openssl rand -base64 64
JWT_ACCESS_SECRET=REPLACE_WITH_64_BYTE_BASE64_SECRET
JWT_REFRESH_SECRET=REPLACE_WITH_64_BYTE_BASE64_SECRET

# Database - Generate with: openssl rand -base64 32 | tr -dc 'a-zA-Z0-9'
DATABASE_PASSWORD=REPLACE_WITH_SECURE_PASSWORD

# =============================================================================
# SMTP Email Configuration (Resend)
# =============================================================================
# 1. Create account at https://resend.com (GitHub login = instant approval)
# 2. Add domain: Domains -> Add Domain -> demiurge.cloud
# 3. Add DNS records they provide
# 4. Create API Key: API Keys -> Create API Key
# =============================================================================
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USERNAME=resend
SMTP_PASSWORD=re_YOUR_RESEND_API_KEY_HERE
SMTP_FROM_EMAIL=noreply@demiurge.cloud
SMTP_FROM_NAME=Demiurge Blockchain
BASE_URL=https://demiurge.cloud
