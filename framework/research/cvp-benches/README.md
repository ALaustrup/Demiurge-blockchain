# CVP benches
Stale criterion benchmark for the CVP proof generator, moved out of the build during Phase 0 because it targets an older API. Re-add as a [[bench]] in modules/cvp/Cargo.toml if revived.
